services:
  qdrant:
    image: qdrant/qdrant:v1.12.4
    container_name: cca_qdrant
    ports:
      - "6333:6333"  # HTTP / REST
      - "6334:6334"  # gRPC
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage:
